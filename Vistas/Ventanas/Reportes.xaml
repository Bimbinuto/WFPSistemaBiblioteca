<UserControl x:Class="Biblioteca.Vistas.Ventanas.Reportes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Biblioteca.Vistas.Ventanas"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Label
            Grid.Row="0"
            Content="Reportes"
            Style="{StaticResource StyleLabelTitle}" />

        <StackPanel
            Grid.Row="1"
            Orientation="Vertical"
            VerticalAlignment="Center">

            <Button
                Style="{StaticResource ButtonLogin}"
                Content="Generar reporte"
                Width="150" 
                Click="btnGenerarReporte"/>

            <Label
                Content="{Binding ResultadoReporte, UpdateSourceTrigger=PropertyChanged}"
                Style="{StaticResource StyleLabel}"
                Height="30" 
                HorizontalAlignment="Center"
                Margin="20"/>

        </StackPanel>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
    </Grid>
</UserControl>
